# PAnDa

This repository is initialized and ready for your first GitHub push.

## What is included

- A standalone Git repository with a `main` branch
- A general-purpose `.gitignore`
- A `.gitattributes` file for sane line-ending handling
- A push guide in `PUSH_TO_GITHUB.md`

## Next

Add your project files, then follow the steps in `PUSH_TO_GITHUB.md`.
